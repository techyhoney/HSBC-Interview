# Browser-facing API URLs (client components)
NEXT_PUBLIC_BFF_URL=http://localhost:8001
NEXT_PUBLIC_MARKET_URL=http://localhost:8080

# Server-side (RSC) URL for the Java market service.
# In docker-compose this is set to the internal service name.
MARKET_API_INTERNAL=http://localhost:8080
