spring.application.name=market-service
server.port=8080

# Task 1 model API base URL (overridden in docker-compose).
model.api.url=http://localhost:8000

# Dataset location on the classpath.
dataset.resource=housing-price-dataset.csv

# Actuator health endpoint.
management.endpoints.web.exposure.include=health
